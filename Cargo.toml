[package]
name = "ferrum_calc"
version = "0.1.0"
edition = "2021"
description = "FerrumCalc — A modern native calculator built with Rust and egui"

[dependencies]
eframe = "0.31"
serde = { version = "1.0", features = ["derive"] }

# Optimize release builds
[profile.release]
opt-level = 2
